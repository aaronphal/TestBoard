package org.usfirst.frc.team537.robot.subsystems;

import edu.wpi.first.wpilibj.Compressor;
import edu.wpi.first.wpilibj.Relay;
import edu.wpi.first.wpilibj.command.Subsystem;
import edu.wpi.first.wpilibj.smartdashboard.SmartDashboard;

/**
 *
 */
public class CompressorThing extends Subsystem {
/*
    // Put methods for controlling this subsystem
    // here. Call these from Commands.
	public Compressor can = new Compressor(0);
	//public Relay thing = new Relay(0);
	
	public void compBool(boolean comp) {
		can.setClosedLoopControl(comp);
	}	
		
	boolean enabled = can.enabled();
	boolean pressureSwitch = can.getPressureSwitchValue();
	double current = can.getCompressorCurrent();
	*/

    public void initDefaultCommand() {
        // Set the default command for a subsystem here.
        //setDefaultCommand(new MySpecialCommand());
    }
}

